<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:risk="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin" tools:context=".MainActivity"
    >

    <TextView
        android:id="@+id/projectsTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingTop="24dp"
        android:textStyle="bold"
        android:textSize="20sp"
        android:text="@string/project_spinner_title"
        />

    <Spinner
        android:id="@+id/projectsSpinner"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/projectsTitle"
        android:paddingTop="16dp"
        android:paddingBottom="16dp"
        />

    <TextView
        android:id="@+id/riskTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/projectsSpinner"
        android:paddingBottom="8dp"
        android:textStyle="bold"
        android:textSize="16sp"
        android:gravity="center"
        android:text="@string/risk_view_title"
        />

    <RelativeLayout
        android:layout_below="@+id/riskTitle"
        android:layout_width="match_parent"
        android:layout_height="200dp"
        >

        <se.frostdigital.riskanalysis.RiskAnalysisBackgroundView
            android:id="@+id/riskAnalysis"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            risk:rows="@integer/risk_rows"
            risk:columns="@integer/risk_columns"
            />

        <se.frostdigital.riskanalysis.RiskAnalysisOverlayView
            android:id="@+id/riskAnalysisOverlay"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            risk:rows="@integer/risk_rows"
            risk:columns="@integer/risk_columns"
            />

    </RelativeLayout>

</RelativeLayout>
